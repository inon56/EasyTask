import { NgModule } from '@angular/core';

// declaration: on this we register all components, directives and pipes that belong to this module
// imports: on this we register all the modules that we want to use in this module
// providers: on this we register all the services that we want to use in this module
// bootstrap: on this we register the root component that Angular creates and inserts into the index.html host web page

@NgModule({
  declarations: [],
  imports: [],
  providers: [],
  bootstrap: []
})

export class AppModule {}